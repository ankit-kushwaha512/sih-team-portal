import Link from "next/link"; import data from "../../data/site.json"; import { ExternalLink, BookOpen } from "lucide-react";
export default function Resources(){return <main className="subpage"><nav className="nav"><Link className="brand" href="/"><span className="logo">S</span>SIH Team Hub</Link></nav><div className="wrap"><section className="subhero"><div className="eyebrow">Resource grove</div><h1>Resources</h1><p className="muted">Curated learning links. Admins can add, edit or remove resources from the CMS in the production backend.</p></section><section className="section" style={{paddingTop:10}}><div className="grid">{data.resources.map(r=><a className="card" href={r.url} target="_blank" rel="noreferrer" key={r.title}><div className="icon"><BookOpen size={21}/></div><h2>{r.title}</h2><p className="muted">{r.tag}</p><div className="tag">{r.type} <ExternalLink size={13} style={{verticalAlign:"middle"}}/></div></a>)}</div></section></div></main>}