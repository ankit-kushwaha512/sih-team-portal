 "use client"; import {useState} from "react"; import Link from "next/link"; import data from "../../data/site.json"; import {ArrowRight, CheckCircle2} from "lucide-react";
export default function Register(){const [role,setRole]=useState(""); return <main className="subpage"><nav className="nav"><Link className="brand" href="/"><span className="logo">S</span>SIH Team Hub</Link><Link className="muted" href="/login">Already registered? Login</Link></nav><div className="wrap"><div className="form"><div className="eyebrow">Join the constellation</div><h1>Create member account</h1><p className="muted">Select your responsibility. The server will use this role to personalise your dashboard, study plan and duties.</p><div className="field"><label>Full name</label><input placeholder="Your name"/></div><div className="field"><label>Email</label><input type="email" placeholder="you@example.com"/></div><div className="field"><label>Password</label><input type="password" placeholder="Create a strong password"/></div><h3 style={{marginTop:28}}>Choose your role</h3><div className="role-grid">{data.roles.map(r=><div className={"card role-card "+(role===r.id?"selected":"")} onClick={()=>setRole(r.id)} key={r.id}><span className="check">{role===r.id&&<CheckCircle2 size={18}/>}</span><b>{r.name}</b><p className="muted" style={{fontSize:13}}>{r.focus.slice(0,2).join(" · ")}</p></div>)}</div><button className="btn primary" style={{marginTop:20,width:"100%",justifyContent:"center"}} onClick={()=>alert(role?"Demo registration selected. Connect this form to the production API/database.":"Select a role first.")}>Create account <ArrowRight size={16}/></button></div></div></main>}