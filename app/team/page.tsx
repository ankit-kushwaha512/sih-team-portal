import Link from "next/link"; import data from "../../data/site.json"; import { Crown, Monitor, Server, Database, Presentation, PanelsTopLeft } from "lucide-react";
const icons:any={Monitor,PanelsTopLeft,Server,Database,Presentation,Crown};
export default function Team(){return <main className="subpage"><nav className="nav"><Link className="brand" href="/"><span className="logo">S</span>SIH Team Hub</Link></nav><div className="wrap"><section className="subhero"><div className="eyebrow">Team constellation</div><h1>Six roles, one mission.</h1><p className="muted">The team structure is editable by an administrator for future SIH cycles.</p></section><section className="section" style={{paddingTop:10}}><div className="grid">{data.roles.map(r=>{const I=icons[r.icon];return <div className="card" key={r.id}><div className="icon"><I size={21}/></div><h2>{r.name}</h2><p className="muted">{r.focus.join(" · ")}</p></div>})}</div></section></div></main>}