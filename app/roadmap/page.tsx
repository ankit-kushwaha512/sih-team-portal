import Link from "next/link"; import data from "../../data/site.json"; import { Mountain, ArrowRight } from "lucide-react";
export default function Roadmap(){return <main className="subpage"><nav className="nav"><Link className="brand" href="/"><span className="logo">S</span>SIH Team Hub</Link></nav><div className="wrap"><section className="subhero"><div className="eyebrow">Mountain trail</div><h1>Team Roadmap</h1><p className="muted">Move from understanding the challenge to a confident final pitch.</p></section><section className="section" style={{paddingTop:10}}>{data.roadmap.map((p,i)=><div className="panel" style={{marginBottom:16,display:"flex",gap:24,alignItems:"flex-start"}} key={p.phase}><div className="icon" style={{minWidth:54}}><Mountain size={22}/></div><div style={{flex:1}}><div className="eyebrow">Phase {p.phase}</div><h2>{p.title}</h2><p className="muted">{p.text}</p><div className="progress"><i style={{width:`${(i+1)/data.roadmap.length*100}%`}}/></div></div></div>)}</section></div></main>}