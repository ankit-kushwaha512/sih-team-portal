import Link from "next/link"; import data from "../../data/site.json"; import { BookOpen, ArrowRight } from "lucide-react";
export default function Study(){return <main className="subpage"><nav className="nav"><Link className="brand" href="/"><span className="logo">S</span>SIH Team Hub</Link><Link className="btn" href="/register">Get started</Link></nav><div className="wrap"><section className="subhero"><div className="eyebrow">Learning forest</div><h1>Study Hub</h1><p className="muted">Choose a role to see the skills that matter most for the SIH build.</p></section><section className="section" style={{paddingTop:10}}><div className="role-grid">{data.roles.map(r=><div className="card" key={r.id}><div className="icon"><BookOpen size={21}/></div><h2>{r.name}</h2><p className="muted">Build a practical foundation around:</p>{r.focus.map(x=><div key={x} style={{padding:"8px 0",borderBottom:"1px solid var(--line)"}}>✓ {x}</div>)}<Link className="btn" style={{marginTop:18}} href="/register">Select role <ArrowRight size={15}/></Link></div>)}</div></section></div></main>}